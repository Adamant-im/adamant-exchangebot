const api = require('../../modules/api');
module.exports = {
	unix() {
		return new Date().getTime();
	},
	getAddressForHashTx(data) {
		return new Promise(function (resolve, reject) {
// api[data.]
		});
	}
};